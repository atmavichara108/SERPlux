# SERPlux v1.0 — Acceptance Checklist

Дата фиксации: 2026-08-17
Target commit: `bdc9a54`

## Подтверждено историей и кодом

- [x] Пайплайн Topvisor → collector → labeler → export → report присутствует.
- [x] Поддержана мультиклиентность через профиль клиента и `client_id`.
- [x] `label_only` реализован в webhook-пайплайне без повторного сбора.
- [x] Накопительный отчёт вставляет новые версии сверху и хранит максимум 10.
- [x] Geo dedup отображаемых алиасов реализован в `reporter.py`.
- [x] Эталон импортируется через `/labels/import` с приоритетом `manual_l1`.
- [x] История до `bdc9a54` подтверждена git log.

## Подтверждено verifier

- [x] `pytest -q` подтверждает все 262 теста: `262 passed in 3.22s`.

## Требует финальной проверки состояния

- [x] Итоговый `git diff --stat` содержит только разрешённые документы и
  pre-existing изменения.
- [x] Итоговый `git status` проверен перед завершением.
- [x] `git tag -l` проверен после тестов.

## Не подтверждено этим документом

- [ ] Боевой прогон с реальными Topvisor, Google Sheets и LLM credentials.
- [ ] Реальное поведение `label_only` на production-БД.
- [ ] Реальная запись накопительного отчёта в Google Sheets.
- [ ] Реальный geo dedup на боевых данных всех клиентов.
- [ ] Полнота и качество эталона для всех клиентов.
- [ ] Безопасность и готовность production deployment без отдельного deploy
  verifier.

Наличие checklist не является доказательством прохождения непроверенных
пунктов.
