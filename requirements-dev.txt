# Зависимости для разработки и тестирования
# Включаются в Docker-образ, чтобы verify.sh мог запускать pytest в контейнере
-r requirements.txt
pytest>=8.0
# Starlette 1.3+ требует httpx2 для TestClient, иначе падает deprecation warning
httpx2>=2.5.0
