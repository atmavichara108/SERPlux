
def export(rows: list[Row]) -> None:
    """
    Пишет в Google Sheet (gspread, GOOGLE_SHEET_ID).
    Лист 'Результаты': дописывает строки (НЕ перезатирает), колонки в порядке:
    Дата | ПС | Запрос | Гео | Позиция | URL | Домен | Метка.
    Цвет строки по label: positive=зелёный, negative=красный, иначе без цвета.
    Лист 'Позитивные' и 'Негативные': обновляет списки уникальных url.
    """
